1. Install JDK 17.
2. Copy .env.example to .env and fill your real values.
3. Open terminal inside backend folder.
4. Run: gradlew.bat bootRun

If tests fail because MongoDB is not running, use:
gradlew.bat bootRun
and for build without tests:
gradlew.bat build -x test
