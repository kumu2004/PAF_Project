package com.smartcampus.backend.module.booking.dto;

import lombok.Data;

@Data
public class ApproveBookingRequest {
    // Currently empty as approval might just need the ID from the URL, 
    // but included for consistency in the API structure.
}
